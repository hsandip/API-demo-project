diff --git a/lib/app.js b/lib/app.js
index f33d8b9f70ab4eeb7c8df8f2fd090a274bffe2bf..61b533199f07da5316f59003960517f4d3a3ea51 100644
--- a/lib/app.js
+++ b/lib/app.js
@@ -91,8 +91,8 @@ export function createApp(db, options = {}) {
         })(req, res, next);
     })
         .options('*', cors());
-    // Body parser
-    app.use(json());
+    // Body parser (raised from milliparsec's 100KiB default to fit base64-encoded image uploads)
+    app.use(json({ payloadLimit: 10 * 1024 * 1024 }));
     app.get('/', (_req, res) => res.send(eta.render('index.html', { data: db.data })));
     app.get('/:name', (req, res, next) => {
         const { name = '' } = req.params;
diff --git a/lib/service.js b/lib/service.js
index d6a3dad3fe1cd5f7a1c8f8b8c5c0e5c8e5f2b1a4..a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0 100644
--- a/lib/service.js
+++ b/lib/service.js
@@ -104,7 +104,13 @@
         const items = this.#get(name);
         if (items === undefined || !Array.isArray(items))
             return;
-        const item = { ...data, id: randomId() };
+        // Honor a caller-supplied id (e.g. a client-generated numeric id) as
+        // long as it's a non-empty string that doesn't collide with an
+        // existing item; otherwise fall back to the default random id.
+        const hasUsableSuppliedId = typeof data.id === 'string' &&
+            data.id.length > 0 &&
+            !items.some((item) => item.id === data.id);
+        const item = { ...data, id: hasUsableSuppliedId ? data.id : randomId() };
         items.push(item);
         await this.#db.write();
         return item;
